# Proof of concepts 

that matter in the project, and need to be isolated , because will be very dificult to debug if we start implementing it in the project, frist we need guaranty it works independent

1. Connect clojure with ollama

		clj -M main.clj

2. Connect python with clojure with ollama  via Jpype


		pip install -r requirements.txt
		python main.py


3. Connect python with clojure with gemmini  via Jpype